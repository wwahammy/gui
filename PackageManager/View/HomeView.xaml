<UserControl x:Class="CoApp.PackageManager.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024" xmlns:my="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:Controls="clr-namespace:CoApp.PackageManager.Controls"
             xmlns:ViewModel="clr-namespace:CoApp.PackageManager.ViewModel">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
        <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Center"  Margin="120,0,0,0" Height="600" ItemsSource="{Binding PanoramaItems}">
                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ItemsPresenter/>
                    </ControlTemplate>
                </ItemsControl.Template>
            
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type ViewModel:PanoramaItemSource}">
                        <Controls:CustomPanoramaItem Header="{Binding Name}">
                            <Controls:TileGridControl 
                                TopLeftContent="{Binding TopLeft}" BottomRightContent="{Binding BottomRight}" TopRightContent="TopRight" BottomLeftContent="{Binding BottomLeft}" BottomMiddleContent="{Binding BottomCenter}">

                            </Controls:TileGridControl>
                        </Controls:CustomPanoramaItem>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>
    </Grid>
</UserControl>
